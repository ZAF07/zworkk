{
  "name": "deployreact",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm install && npm start",
    "heroku-postbuiild": "cd client && npm install && npm run build",
    "try": "cd server && nodemon server.js && cd client && npm start && npm run build"
  },

  "engines": {
    "node": "12.18.3"
  },

  "keywords": [],
  "author": "",
  "license": "ISC"
}
